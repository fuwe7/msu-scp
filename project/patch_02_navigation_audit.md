# Аудит: Патч навигации и CTA (Patch 02)

## Аудит меню

- **Файл:** `templates/header.html` (и все файлы в `dist/**/*.html`)
- **Удалённый пункт:** "База Объектов" (включая dropdown-меню с "Объекты SCP-MSU" и "Локации (ЛП)")
- **Старый href:** `/templates/hub-template.html` (и относительные пути в сборке)
- **Причина:** Упрощение навигации и удаление заглушек согласно заданию
- **Подтверждение:** Блок полностью вырезан из `templates/header.html` и всех сгенерированных страниц в каталоге `dist/`.

- **Файл:** `templates/header.html` (и все файлы в `dist/**/*.html`)
- **Удалённый пункт:** "Архив Инцидентов и Рассказов"
- **Старый href:** `/templates/tales-hub.html` (и относительные пути в сборке)
- **Причина:** Упрощение навигации и удаление заглушек согласно заданию
- **Подтверждение:** Элемент списка `<li>` вырезан из `templates/header.html` и всех сгенерированных страниц в каталоге `dist/`.

**Стилевые правки меню:** 
Header (`.site-header`) переведён на `display: flex`, чтобы логотип и навигация равномерно и красиво выстраивались без пустых промежутков после удаления пунктов меню. У логотипа (`.logo-container`) убрана нижняя рамка для лучшего выравнивания.

## Аудит CTA (Call to Action)

- **Файл главной страницы:** `dist/index.html` (и исходный `index.html`)
- **Текущий элемент:** `<a href="pages/tale-intro.html">Читать полностью >></a>`
- **href:** `pages/tale-intro.html`
- **Класс:** `.cta-archive`
- **Запланированное изменение:** Превратить текстовую ссылку-плейсхолдер в полноценную CTA-кнопку.
- **Подтверждение:** В `dist/index.html` ссылка заменена на `<a href="pages/tale-intro.html" class="cta-archive">Читать полностью</a>`. В корневом `index.html` плейсхолдер `{{INTRO_TALE}}` заменён на аналогичный обновлённый HTML-код с кнопкой, чтобы изменения сохранились при последующих сборках. В файлы `css/style.css` и `dist/css/style.css` добавлены соответствующие стили: прямоугольная форма, красная акцентная рамка/текст палитры К.И.П.П., uppercase, hover-эффекты.
